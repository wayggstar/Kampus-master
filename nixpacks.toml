[variables]
NIXPACKS_JDK_VERSION = "17"
_JAVA_OPTIONS = "-Xmx384m -XX:MaxMetaspaceSize=192m"

[phases.install]
cmds = ["chmod +x gradlew"]

[phases.build]
cmds = ["./gradlew clean build -x test --no-daemon"]

[start]
cmd = "java -jar build/libs/*.jar"